services:
  - type: web
    name: codesage
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: CODESAGE_TARGET_REPO
        value: /app/target_repo
